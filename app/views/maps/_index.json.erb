{ "add":
[ <% for map in @add_maps %>
{ "id": <%= map.id %>,
  "group":"<%= map.group.gsub("\"","\\\"") %>",	
  "members":"<%= map.members.gsub("\"","\\\"") %>",
  "project":"<%= map.project.gsub("\"","\\\"") %>",
  "contact":"<%= map.contact.gsub("\"","\\\"") %>",
  "x":  <%= map.x %>,
  "y":  <%= map.y %> },
<% end %>
{}],
"del":
[ <% for map in @del_maps %>
<%= map %>,
<% end %>
0]
}
